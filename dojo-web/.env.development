# 【开发】环境变量

# 应用部署基础路径（如部署在子目录 /admin，则设置为 /admin/）
VITE_BASE_URL = /

# API 请求基础路径（开发环境设置为 / 使用代理，生产环境设置为完整后端地址）
VITE_API_URL = /

# Art Design Pro 登录/菜单 Mock（保留壳登录能力）
VITE_API_PROXY_URL = https://m1.apifoxmock.com/m1/6400575-6097373-default

# Dojo FastAPI
VITE_DOJO_PROXY_URL = http://127.0.0.1:8000

# Dojo API 前缀（走 Vite /api/dojo 代理）
VITE_DOJO_API_BASE = /api/dojo

# Delete console
VITE_DROP_CONSOLE = false
